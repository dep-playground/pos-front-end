export class Item{
    constructor(public code: string, public description: string, public qty: number, public unitprice: string){
    }
}

export class upItem{
    constructor(public description: string, public qty: number, public unitprice: string){
    }
}